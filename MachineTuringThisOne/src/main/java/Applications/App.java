package Applications;

import Model.TuringMachine;
import Model.TuringMachineException;
import View.View;

public class App {
    public static void main(String[] args) {
        /*View view = new View();
        Application application = new Application(view);
        view.displayLevels(application.getGameLevels());
        application.setFacade(new TuringMachine(application.askLevel()));


        while (true) {
            try {
                application.askInput();
            } catch (TuringMachineException e) {
                System.out.println(e.getMessage());
            }
        }
         */
    }
}
